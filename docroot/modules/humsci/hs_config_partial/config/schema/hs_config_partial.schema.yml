hs_config_partial.settings:
  type: config_object
  label: 'HS Config Partial settings'
  mapping:
    enabled:
      type: boolean
      label: 'Enable partial import protection'
      description: 'If TRUE, prevents config deletions during import.'
