project:
  machine_name: language
  human_name: language
  local:
    protocol: http
    hostname: local.language.com
drush:
  aliases:
    local: language.local
    remote: language.prod
drupal:
  db: {  }
