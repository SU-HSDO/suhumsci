import equalHeightGrid from './equal-height-grid';

// Make the vertical linked card titles the same max height
equalHeightGrid('hb-vertical-linked-card__title', 'hb-vertical-linked-card--equal');

// TODO NOTE that we can REMOVE
// also rather than doing the CSS I wrote, this could also work to make the card description the 
// same height:
// equalHeightGrid('hb-vertical-linked-card', 'hb-vertical-linked-card--equal');

