diff --git a/src/Plugin/Purge/Invalidation/UrlInvalidation.php b/src/Plugin/Purge/Invalidation/UrlInvalidation.php
index cbe8344..eff3683 100644
--- a/src/Plugin/Purge/Invalidation/UrlInvalidation.php
+++ b/src/Plugin/Purge/Invalidation/UrlInvalidation.php
@@ -81,7 +81,7 @@ class UrlInvalidation extends InvalidationBase implements InvalidationInterface
       throw new InvalidExpressionException('Scheme unsupported!');
     }
     if (!UrlHelper::isValid($url, TRUE)) {
-      throw new InvalidExpressionException('The URL is invalid.');
+      throw new InvalidExpressionException('The URL is invalid -- ' . $url);
     }
     if ($this->wildCardCheck && (strpos($url, '*') !== FALSE)) {
       throw new InvalidExpressionException('URL invalidations should not contain asterisks!');
