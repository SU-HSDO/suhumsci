diff --git a/redirect.services.yml b/redirect.services.yml
index 908b399ec7683cb7b74a4a67fe91dd5921779b1e..2b2d87c298465f4dd1a9660a8728aff8e1160d61 100644
--- a/redirect.services.yml
+++ b/redirect.services.yml
@@ -7,7 +7,7 @@ services:
   Drupal\redirect\RedirectPrefixList: '@redirect.prefix_list'
   redirect.repository:
     class: Drupal\redirect\RedirectRepository
-    arguments: ['@entity_type.manager', '@database', '@config.factory', '@request_stack', '@redirect.prefix_list']
+    arguments: ['@entity_type.manager', '@database', '@config.factory', '@request_stack', '@redirect.prefix_list', '@logger.factory']
     tags:
       - { name: backend_overridable }
   Drupal\redirect\RedirectRepository: '@redirect.repository'
diff --git a/src/RedirectRepository.php b/src/RedirectRepository.php
index ea438569eced45ad6297878dc75a25edf582ff49..4ce42d505fbd7df710f38e34781b67038fe8aaf9 100644
--- a/src/RedirectRepository.php
+++ b/src/RedirectRepository.php
@@ -7,8 +7,10 @@ use Drupal\Core\Config\ConfigFactoryInterface;
 use Drupal\Core\Database\Connection;
 use Drupal\Core\Entity\EntityTypeManagerInterface;
 use Drupal\Core\Language\Language;
+use Drupal\Core\Language\LanguageInterface;
+use Drupal\Core\Logger\LoggerChannelFactoryInterface;
 use Drupal\redirect\Entity\Redirect;
-use Drupal\redirect\Exception\RedirectLoopException;
+use Symfony\Component\HttpKernel\Exception\NotFoundHttpException;
 use Symfony\Component\HttpFoundation\RequestStack;
 
 /**
@@ -17,26 +19,32 @@ use Symfony\Component\HttpFoundation\RequestStack;
 class RedirectRepository {
 
   /**
+   * The entity type manager service.
+   *
    * @var \Drupal\Core\Entity\EntityTypeManagerInterface
    */
   protected $manager;
 
   /**
+   * The current database connection.
+   *
    * @var \Drupal\Core\Database\Connection
    */
   protected $connection;
 
   /**
+   * The configuration for this module's settings.
+   *
    * @var \Drupal\Core\Config\ImmutableConfig
    */
   protected $config;
 
   /**
-   * An array of found redirect IDs to avoid recursion.
+   * The logger service.
    *
-   * @var array
+   * @var \Drupal\Core\Logger\LoggerChannelInterface
    */
-  protected $foundRedirects = [];
+  protected $logger;
 
   /**
    * The request stack.
@@ -56,13 +64,15 @@ class RedirectRepository {
    * @param \Drupal\Core\Database\Connection $connection
    *   The database connection.
    * @param \Drupal\Core\Config\ConfigFactoryInterface $config_factory
-   *   The config factory.
+   *   The configuration factory service.
    * @param \Symfony\Component\HttpFoundation\RequestStack|null $request_stack
-   *   The request stack.
+   *   The request stack for the current requeset.
    * @param \Drupal\redirect\RedirectPrefixList|null $prefix_list
-   *   The prefix list service.
+   *   The redirect prefix list service.
+   * @param \Drupal\Core\Logger\LoggerChannelFactoryInterface|null $logger_factory
+   *   The message logger.
    */
-  public function __construct(EntityTypeManagerInterface $manager, Connection $connection, ConfigFactoryInterface $config_factory, ?RequestStack $request_stack = NULL, ?RedirectPrefixList $prefix_list = NULL) {
+  public function __construct(EntityTypeManagerInterface $manager, Connection $connection, ConfigFactoryInterface $config_factory, ?RequestStack $request_stack = NULL, ?RedirectPrefixList $prefix_list = NULL, ?LoggerChannelFactoryInterface $logger_factory = NULL) {
     $this->manager = $manager;
     $this->connection = $connection;
     $this->config = $config_factory->get('redirect.settings');
@@ -78,6 +88,12 @@ class RedirectRepository {
       $prefix_list = \Drupal::service('redirect.prefix_list');
     }
     $this->prefixList = $prefix_list;
+    if (!$logger_factory) {
+      @trigger_error('Calling ' . __METHOD__ . ' without the $prefix_list argument is deprecated in redirect:1.12.0 and it will be required in redirect:2.0.0. See https://www.drupal.org/project/redirect/issues/3451531', E_USER_DEPRECATED);
+      // @phpstan-ignore globalDrupalDependencyInjection.useDependencyInjection
+      $logger_factory = \Drupal::service('logger.factory');
+    }
+    $this->logger = $logger_factory->get('redirect');
   }
 
   /**
@@ -91,33 +107,36 @@ class RedirectRepository {
    *   The language for which is the redirect.
    * @param \Drupal\Core\Cache\CacheableMetadata|null $cacheable_metadata
    *   The cacheable metadata for all the redirects involved.
+   * @param array $found_redirects
+   *   The list of redirects found.
    *
    * @return \Drupal\redirect\Entity\Redirect|null
    *   The matched redirect entity or NULL if no redirect was found.
    *
-   * @throws \Drupal\redirect\Exception\RedirectLoopException
+   * @throws \Symfony\Component\HttpKernel\Exception\NotFoundHttpException
+   *   Thrown if a redirect loop is discovered.
    */
-  public function findMatchingRedirect($source_path, array $query = [], $language = Language::LANGCODE_NOT_SPECIFIED, ?CacheableMetadata $cacheable_metadata = NULL) {
+  public function findMatchingRedirect($source_path, array $query = [], $language = Language::LANGCODE_NOT_SPECIFIED, ?CacheableMetadata $cacheable_metadata = NULL, array $found_redirects = []): ?Redirect {
     $source_path = ltrim($source_path, '/');
 
     // If the source path has at least two path components, check if the
     // first part does have any redirects. This saves a lot of lookups for sites
     // that don't have any redirects on common prefixes such as /node or
     // /admin.
-    if ($this->prefixList && !$this->prefixList->hasRedirectsWithPrefix($source_path)) {
+    if (!$this->prefixList->hasRedirectsWithPrefix($source_path)) {
       return NULL;
     }
 
     $hashes = [Redirect::generateHash($source_path, $query, $language)];
-    if ($language != Language::LANGCODE_NOT_SPECIFIED) {
-      $hashes[] = Redirect::generateHash($source_path, $query, Language::LANGCODE_NOT_SPECIFIED);
+    if ($language != LanguageInterface::LANGCODE_NOT_SPECIFIED) {
+      $hashes[] = Redirect::generateHash($source_path, $query, LanguageInterface::LANGCODE_NOT_SPECIFIED);
     }
 
     // Add a hash without the query string if using passthrough_querystring.
     if (!empty($query) && $this->config->get('passthrough_querystring')) {
       $hashes[] = Redirect::generateHash($source_path, [], $language);
-      if ($language != Language::LANGCODE_NOT_SPECIFIED) {
-        $hashes[] = Redirect::generateHash($source_path, [], Language::LANGCODE_NOT_SPECIFIED);
+      if ($language != LanguageInterface::LANGCODE_NOT_SPECIFIED) {
+        $hashes[] = Redirect::generateHash($source_path, [], LanguageInterface::LANGCODE_NOT_SPECIFIED);
       }
     }
 
@@ -133,32 +152,33 @@ class RedirectRepository {
 
     if (!empty($rid)) {
       // Check if this is a loop.
-      if (in_array($rid, $this->foundRedirects)) {
-        throw new RedirectLoopException('/' . $source_path, $rid);
+      if (in_array($rid, $found_redirects)) {
+        $this->logger->error(
+          'Redirect loop identified at %path for redirect %rid',
+          ['%path' => $source_path, '%rid' => $rid],
+        );
+        throw new NotFoundHttpException();
       }
+      $found_redirects[] = $rid;
+
       $redirect = $this->load($rid);
       // The redirect could be found by the direct db query in ::findByHash(),
       // but other modules (e.g. Trash) might prevent it from being loaded.
       if (!$redirect instanceof Redirect) {
         return NULL;
       }
-      $this->foundRedirects[] = $rid;
       if ($cacheable_metadata) {
         $cacheable_metadata->addCacheableDependency($redirect);
       }
 
       // Find chained redirects.
-      if ($recursive = $this->findByRedirect($redirect, $language, $cacheable_metadata)) {
-        // Reset found redirects.
-        $this->foundRedirects = [];
+      if ($recursive = $this->findByRedirect($redirect, $language, $cacheable_metadata, $found_redirects)) {
         return $recursive;
       }
 
       return $redirect;
     }
 
-    // Reset found redirects.
-    $this->foundRedirects = [];
     return NULL;
   }
 
@@ -171,8 +191,13 @@ class RedirectRepository {
    *   The language to use.
    * @param \Drupal\Core\Cache\CacheableMetadata|null $cacheable_metadata
    *   The cacheable metadata for all the redirects involved.
+   * @param array $found_redirects
+   *   The list of found redirects.
+   *
+   * @return \Drupal\redirect\Entity\Redirect|null
+   *   The matched redirect entity or NULL if no redirect was found.
    */
-  protected function findByRedirect(Redirect $redirect, $language, ?CacheableMetadata $cacheable_metadata = NULL) {
+  protected function findByRedirect(Redirect $redirect, string $language, ?CacheableMetadata $cacheable_metadata = NULL, array $found_redirects = []): ?Redirect {
     $uri = $redirect->getRedirectUrl();
     $base_url = $this->requestStack->getCurrentRequest()->getBaseUrl();
     if ($uri->isRouted()) {
@@ -185,7 +210,7 @@ class RedirectRepository {
 
     }
     $query = $uri->getOption('query') ?: [];
-    return $this->findMatchingRedirect($path, $query, $language, $cacheable_metadata);
+    return $this->findMatchingRedirect($path, $query, $language, $cacheable_metadata, $found_redirects);
   }
 
   /**
diff --git a/tests/src/Functional/RedirectUITest.php b/tests/src/Functional/RedirectUITest.php
index 08f80c780bdaa00847ca0da2b1aeaed314af670c..2ee73add02b80d36719efae7c22b61c4677a31f3 100644
--- a/tests/src/Functional/RedirectUITest.php
+++ b/tests/src/Functional/RedirectUITest.php
@@ -224,8 +224,7 @@ class RedirectUITest extends BrowserTestBase {
 
     $this->maximumRedirects = 10;
     $this->drupalGet('node');
-    $this->assertSession()->pageTextContains('Service unavailable');
-    $this->assertSession()->statusCodeEquals(503);
+    $this->assertSession()->statusCodeEquals(404);
 
     $log = \Drupal::database()->select('watchdog')->fields('watchdog')->condition('type', 'redirect')->execute()->fetchAll();
     if (count($log) == 0) {
diff --git a/tests/src/Kernel/RedirectAPITest.php b/tests/src/Kernel/RedirectAPITest.php
index 9d1fa4e2f8d96f592b37aa62fdb70e487d1e3955..d26339ccc3ddc181eff9fe6adc988881617c8a1c 100644
--- a/tests/src/Kernel/RedirectAPITest.php
+++ b/tests/src/Kernel/RedirectAPITest.php
@@ -7,7 +7,6 @@ namespace Drupal\Tests\redirect\Kernel;
 use Drupal\language\Entity\ConfigurableLanguage;
 use Drupal\redirect\Entity\Redirect;
 use Drupal\Core\Language\Language;
-use Drupal\redirect\Exception\RedirectLoopException;
 use Drupal\KernelTests\KernelTestBase;
 
 /**
@@ -291,11 +290,15 @@ class RedirectAPITest extends KernelTestBase {
     // Create a loop.
     $one->setRedirect('third-path');
     $one->save();
-    try {
-      $repository->findMatchingRedirect('third-path');
-      $this->fail('Failed to detect a redirect loop.');
+    // This is counter-intuitive but since #3018897 this is better behaviour
+    // than throwing a RedirectLoopException.
+    $found = $repository->findMatchingRedirect('third-path');
+    if (!empty($found)) {
+      $this->assertEquals($found->getRedirectUrl()->toString(), '/node',
+        'Redirect loop "properly" resolved in findMatchingRedirect without looping.');
     }
-    catch (RedirectLoopException) {
+    else {
+      $this->fail('Failed to resolve a redirect loop.');
     }
   }
 
