<?xml version="1.0"?>
<ruleset name="Stanford Project Standard" namespace="Stanford\project\CS\Standard">
  <config name="installed_paths" value="../../drupal/coder/coder_sniffer/,../../sirbrillig/phpcs-variable-analysis/,../../slevomat/coding-standard/" />

  <!-- Use colors in output. -->
  <arg name="colors"/>
  <!-- Show progress. -->
  <arg value="p"/>

  <arg name="ignore" value="*.css,*.md,*.txt,*.png,*.gif,*.jpeg,*.jpg,*.svg"/>
  <arg name="extensions" value="php,module,inc,install,test,profile,theme,info,yml"/>

  <rule ref="Drupal">
    <exclude name="Drupal.Commenting.FunctionComment.MissingReturnComment"/>
    <exclude name="Drupal.Commenting.DocComment.MissingShort"/>
    <exclude name="Drupal.Commenting.FunctionComment.MissingParamComment"/>
    <!-- Custom modules and themes aren't packaged with drupal.org. Remove these
    warnings.  -->
    <exclude name="Drupal.InfoFiles.AutoAddedKeys.Project"/>
    <exclude name="Drupal.InfoFiles.AutoAddedKeys.Version"/>
  </rule>

  <rule ref="Drupal.Files.EndFileNewline.NoneFound">
    <!--
      Multiline config exports like user.mail.yml do not end with a blank line.
      This is not a functional issue, but PHPCS reports it and it is
      time-consuming to fix after every config export. Until the upstream issue
      is resolved, it is easier to ignore this rule for this file.
      See:
      * https://www.drupal.org/project/drupal/issues/3279043
      * https://www.drupal.org/project/drupal/issues/3240104
    -->
    <exclude-pattern>*config/*/user.mail.yml</exclude-pattern>
  </rule>

  <file>docroot/profiles/humsci</file>
  <file>docroot/modules/humsci</file>
  <file>docroot/themes/humsci</file>

  <exclude-pattern>*/tests/*</exclude-pattern>
  <exclude-pattern>*/node_modules/*</exclude-pattern>
  <exclude-pattern>*/dist/*</exclude-pattern>
</ruleset>
